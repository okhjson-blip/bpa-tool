# 과제정보 CSV 양식

## 목적

결과 리포트 화면의 `과제정보 CSV 출력`은 과제당 한 행의 UTF-8 CSV를 만듭니다. PDF 결과 리포트의 STATIK·참여자·AS-IS·BDW·AI FIT·To-Be·AX 성과지표를 모두 포함합니다. As-Is와 To-Be는 플로우차트와 같은 순서로 각 L6 노드의 작업방식·도구·수행시간을 포함합니다.

## 열 정의

| 열 | 권장 DB 형식 | 설명 |
|---|---|---|
| `과제명` | text | 과제명 |
| `시작일` | date | 과제 시작일 |
| `완료일` | date | 과제 종료일 |
| `성과목표` | text | 과제 목표 |
| `프로젝트명` | text | 프로젝트명 |
| `L1 구분` | text | STATIK L1 |
| `L2 대분류` | text | STATIK L2 |
| `L3 중분류` | text | STATIK L3 |
| `L4 모듈` | text | STATIK L4 |
| `과제 참여자` | text | `이름 / 직급 / 역할 / 이메일`을 ` > `로 연결 |
| `As-Is` | text | L6 Act를 `이름 [작업방식 \| 도구 \| h:s]` 형식으로 ` > `로 연결 |
| `AS-IS 상세` | text | 수행시간·대기(h)·승인대기(h)를 포함한 AS-IS 행 |
| `BDW 요약` | text | 병목·Delay·Waste 건수 |
| `BDW 상세` | text | `이름 [태그]`를 ` > `로 연결 |
| `AI FIT` | text | 추천 기술·난이도·가능성·비효율·분류·절감시간을 ` > `로 연결 |
| `To-Be` | text | As-Is와 같은 순서. AI 수락 노드는 `AI 자동화`와 추천 도구·예상 수행시간 |
| `To-Be 상세` | text | 구현 난이도를 포함한 To-Be 행. 미수락은 `-` |
| `난이도` | text | 수락·반영된 AI 자동화의 평균 구현 난이도. `하` / `중` / `상` / `해당 없음` |
| `AS-IS 수행시간` | text | 건당 As-Is 합계(`h:s`) |
| `To-Be 수행시간` | text | 건당 To-Be 합계(`h:s`) |
| `건당 절감시간` | text | `h:s` |
| `시간 절감률` | text | `%` |
| `자동화율` | text | `%` |
| `AI 자동화 구현 난이도` | text | `난이도`와 동일 |
| `수행 빈도` | text | `주 1회 (연간 52회)` 형식 |
| `연간 절감시간` | text | `h:s` |
| `절감 FTE` | numeric | 연간 절감시간 ÷ 2,248 |
| `추정 외주개발비` | text | `₩` + 천단위 구분 |

## 노드 표기

예: `판매 데이터를 검토한다 [수작업 | 엑셀 | 0h:25s] > 보고서를 작성한다 [시스템 | ERP | 0h:10s]`

- 작업방식: `수작업`, `시스템`, To-Be에서 AI가 적용된 노드는 `AI 자동화`
- 도구: `이메일`, `문서`, `엑셀`, `웹`, `ERP`, `기타 도구`. `other`에 직접 입력한 도구명이 있으면 `기타 도구(입력명)`으로 표기
- 수행시간: 시간:분(`12h:30s`). 계산은 분으로 환산한 뒤 다시 `h:s`로 표기. 예: 25분 → `0h:25s`, 88분 → `1h:28s`

## 적재 규칙

1. UTF-8 BOM을 제거한 뒤 헤더가 `과제명`으로 시작하고 `추정 외주개발비`로 끝나는지 확인합니다.
2. 리포트 저장 여부와 관계없이 현재 과제 프로세스와 To-Be·AI FIT·BDW 결과로 생성합니다. 수행 빈도는 `frequency_unit`·`frequency_count` 쿼리로 반영합니다.
3. As-Is·To-Be의 ` > ` 구분 순서는 화면 플로우차트와 같습니다.
4. CSV 셀은 RFC 4180 방식으로 큰따옴표가 이스케이프되며, 스프레드시트 수식 주입 방지를 위해 위험한 일반 텍스트 값은 앞에 작은따옴표가 추가될 수 있습니다.
