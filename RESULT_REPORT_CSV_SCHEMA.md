# 과제정보 CSV 양식

## 목적

결과 리포트 화면의 `과제정보 CSV 출력`은 과제당 한 행의 UTF-8 CSV를 만듭니다. As-Is와 To-Be는 플로우차트와 같은 순서로 각 L6 노드의 작업방식·도구·수행시간을 포함합니다.

## 열 정의

| 열 | 권장 DB 형식 | 설명 |
|---|---|---|
| `과제명` | text | 과제명 |
| `시작일` | date | 과제 시작일 |
| `완료일` | date | 과제 종료일 |
| `성과목표` | text | 과제 목표 |
| `As-Is` | text | L6 Act를 `이름 [작업방식 \| 도구 \| h:s]` 형식으로 ` > `로 연결 |
| `To-Be` | text | As-Is와 같은 순서. AI 수락 노드는 `AI 자동화`와 추천 도구·예상 수행시간 |
| `난이도` | text | 수락·반영된 AI 자동화의 평균 구현 난이도. `하` / `중` / `상` / `해당 없음` |

## 노드 표기

예: `판매 데이터를 검토한다 [수작업 | 엑셀 | 0:25] > 보고서를 작성한다 [시스템 | ERP | 0:10]`

- 작업방식: `수작업`, `시스템`, To-Be에서 AI가 적용된 노드는 `AI 자동화`
- 도구: `이메일`, `문서`, `엑셀`, `웹`, `ERP`, `기타 도구` 또는 추천 자동화 도구명
- 수행시간: 시간:분(`h:s`). 계산은 분으로 환산한 뒤 다시 `h:s`로 표기. 예: 25분 → `0:25`, 88분 → `1:28`

## 적재 규칙

1. UTF-8 BOM을 제거한 뒤 헤더 `과제명,시작일,완료일,성과목표,As-Is,To-Be,난이도`를 확인합니다.
2. 리포트 저장 여부와 관계없이 현재 과제 프로세스와 To-Be 결과로 생성합니다.
3. As-Is·To-Be의 ` > ` 구분 순서는 화면 플로우차트와 같습니다.
4. CSV 셀은 RFC 4180 방식으로 큰따옴표가 이스케이프되며, 스프레드시트 수식 주입 방지를 위해 위험한 일반 텍스트 값은 앞에 작은따옴표가 추가될 수 있습니다.
