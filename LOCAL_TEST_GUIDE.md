# BPA Tool 로컬 테스트 가이드

## 1. 실행

PowerShell에서 프로젝트 루트로 이동한 뒤 실행합니다.

```powershell
cd D:\hjson_tool\bpa-tool
npm.cmd run dev:local
```

환경 점검 후 UI와 API가 함께 실행됩니다.

- UI: http://localhost:3000
- API 상태: http://localhost:5000/api/health
- 종료: `Ctrl+C`

## 2. 첫 화면

1. 관리자 탭에서 Supabase Edge Function Secret으로 설정된 비밀번호를 입력하고 협력사를 등록합니다.
2. 로그아웃한 뒤 협력사 탭에서 등록한 협력사를 선택합니다.
3. 이름과 올바른 형식의 이메일을 입력하고 `시작하기`를 누릅니다. 기존 등록자는 바로 접속되고, 미등록 사용자는 등록 안내와 `등록` 버튼이 표시되는지 확인합니다.
4. 미등록 사용자에게 표시된 `등록` 버튼을 눌렀을 때만 데이터베이스에 프로필·멤버십이 생성되고 프로젝트 목록으로 이동하는지 확인합니다.
5. 이메일 승인 없이 즉시 프로젝트 목록에 진입하는지 확인합니다.
6. 새로고침 후 같은 사용자와 협력사가 자동 복원되는지 확인합니다.
7. 이미 가입된 상태에서 `시작하기`를 다시 눌러도 재로그인 안내 오류 없이 프로젝트 목록으로 이동하는지 확인합니다.
8. 로그인 후 상단 `AI API 관리`로 이동합니다.
9. Gemini, OpenAI, Claude 중 하나의 API Key를 입력하고 `연결 확인 및 저장`을 누릅니다.
10. 새로고침 후에도 등록 상태와 마스킹 힌트가 유지되고 Key 원문은 표시되지 않는지 확인합니다.
11. 두 개 이상 등록한 경우 `기본 엔진 설정`이 새 프로젝트에 반영되는지 확인합니다.

## 3. 권한과 데이터 격리

1. 서로 다른 두 협력사에 협력사 계정을 각각 등록합니다.
2. 협력사 A에서 프로젝트와 과제를 만든 뒤 협력사 B 로그인에서는 표시되지 않는지 확인합니다.
3. 협력사 A 사용자가 협력사 B의 프로젝트 ID를 URL/API에 직접 넣어도 조회되지 않는지 확인합니다.
4. 관리자는 두 협력사의 프로젝트·과제를 모두 조회하되 수정·삭제할 수 없는지 확인합니다.
5. 관리자가 협력사 A를 완료 처리한 뒤 소속 프로젝트·과제가 모두 내부 상태 `suspended`로 변경되고 기존 A 세션의 프로젝트 API가 차단되는지 확인합니다.
6. 관리자 통계에서 협력사·프로젝트·과제의 전체 건수는 유지되고 활성 건수만 감소하는지 확인합니다.
7. 협력사를 다시 활성화하면 프로젝트·과제가 완료 처리 전 상태로 복원되고 기존 세션으로 다시 접근 가능한지 확인합니다.
8. 협력사 모드에서 결과 리포트 프리뷰만 생성했을 때 관리자 상세가 활성화되지 않고, `결과 리포트 저장` 후 관리자 과제 `상세 조회` 팝업에 동일한 리포트와 저장 시각이 표시되는지 확인합니다.
9. 관리자 협력사 등록에서 사업자등록번호가 제거되고 연도·상/하반기가 필수인지 확인합니다.
10. 협력사 목록과 로그인 선택 목록에 컨설팅 차수가 표시되는지 확인합니다.
11. 관리자 `CSV 파일 다운로드` 결과가 협력사명·프로젝트명·과제명으로 구성되고 과제당 한 행인지 확인합니다.
12. 관리자 `협력사/사용자 관리`에서 이메일 형식 검증, 사용자 등록·수정·삭제와 최근 접속 시간 표시가 정상인지 확인합니다.
13. 관리자가 선등록한 이메일로 협력사 로그인을 완료하면 사용자 상태가 `선등록`에서 `로그인 연결`로 바뀌고 최근 접속 시간이 표시되는지 확인합니다.

## 4. 분석 흐름

1. 새 프로젝트를 만듭니다. 프로젝트에는 협력사 설정의 기본 AI 엔진이 저장됩니다.
2. 각 프로젝트 기본정보 필수값을 일부 비운 상태에서 다음 단계가 차단되는지 확인하고, `저장` 후 새로고침했을 때 입력값이 복원되는지 확인합니다.
3. 프로젝트 카드의 `삭제`를 눌러 `삭제하시겠습니까?`와 `네/취소`가 표시되고, `취소` 시 데이터가 유지되는지 확인합니다.
4. L4 모듈 과제를 등록합니다. 프로젝트 기간 밖의 과제 기간은 안내와 함께 차단되어야 합니다.
5. 기존 과제를 다시 열어 이전 단계로 이동했을 때 과제 기본정보가 로딩되고 수정·저장되는지 확인합니다.
6. 인터뷰 답변을 입력해 `저장`·새로고침 복원을 확인한 뒤 AI Draft를 생성합니다. 다음 단계에서 `이전`을 눌렀을 때 저장된 인터뷰 답변과 AI Draft가 다시 표시되어야 합니다.
7. L4 모듈·L5 단위·L6 Act Draft가 표시되고 L6 시간 합계가 L5와 L4에 반영되는지 확인합니다.
8. 프로세스 행을 선택하고 행 추가·위로·아래로·삭제·레벨 변경을 각각 실행합니다. L6에서만 작업방식·도구를 편집할 수 있고 L4·L5는 `—`인지 확인한 뒤 저장·복원과 플로우차트 동기화를 확인합니다.
9. 동기화 후 새로고침해 편집 순서와 추가·삭제가 Supabase 기준으로 유지되고, 플로우차트에서 작업방식·도구 설명은 L6 노드에만 표시되는지 확인합니다.
10. BDW 진단을 실행하고 플로우차트 노드 태그와 진단표가 함께 바뀌며 리스트가 플로우차트 순서이고 즉시 저장 상태가 표시되는지 확인합니다.
11. AI FIT 분석을 실행해 프로세스 순서대로 점수·분류·추천 기술·구현 난이도(상·중·하)와 To-Be 데이터가 생성되는지 확인합니다.
12. As-Is/To-Be 비교 양쪽에 작업방식 배지와 도구·수행시간이 표시되고, As-Is는 원본 작업방식을 유지하며 자동화된 To-Be 노드에 `AI 자동화` 또는 `시스템 자동화` 배지가 표시되는지 확인합니다.

프로젝트에 저장된 엔진의 협력사 API Key가 없으면 분석은 차단됩니다. 상단 `AI API 관리`에서 프로젝트와 같은 엔진의 Key를 등록하세요.

## 5. 결과 리포트

1. 결과 리포트로 이동합니다.
2. 프로젝트명과 과제명이 맞는지 확인합니다.
3. 수행 빈도의 단위와 횟수를 변경하고 `저장` 후 재접속해 값이 복원되며 연간 절감시간과 FTE가 다시 계산되는지 확인합니다.
4. 출력 형식 선택 위의 PDF 프리뷰에 과제 참여자만 이름·직급·역할·이메일로 표시되는지 확인합니다. BDW 요약과 전체 L6 목록, L6 AS-IS·To-Be의 작업방식·도구 컬럼, AI FIT 구현 난이도도 확인합니다.
5. `결과 리포트 저장`을 누르고 저장 완료 시각이 표시되는지 확인합니다.
6. 과제 목록으로 돌아가 해당 과제가 `완료`로 표시되고 관리자 과제 목록도 `완료` 및 상세 조회 활성 상태인지 확인합니다.
7. `전체 리포트 PDF 출력`을 누르고 열린 인쇄 창에서 대상을 `PDF로 저장`으로 선택합니다.
8. `과제정보 CSV 출력`을 누르고 과제당 한 행인지 확인합니다.
9. CSV 헤더가 `과제명, 시작일, 완료일, 성과목표, As-Is, To-Be, 난이도`이고, As-Is·To-Be가 플로우차트와 같은 순서로 각 노드의 작업방식·도구·수행시간을 포함하는지 확인합니다.

## 6. 문제 확인

- 환경 오류: `npm.cmd run doctor`
- Supabase 오류: `http://localhost:5000/api/health` 응답 확인
- 화면 오류: 브라우저 개발자 도구의 Console과 Network 확인
- 포트 충돌: 3000번 또는 5000번을 사용 중인 프로그램을 종료한 후 다시 실행
- AI 오류: 프로젝트 엔진과 첫 화면에서 선택한 엔진이 같은지, 해당 모델 사용 권한이 있는지 확인
- 협력사 등록 실패: Supabase Auth의 Anonymous Sign-Ins 활성화 여부 확인
- 관리자 진입 실패: Supabase Edge Functions의 `admin-password-verify` 배포 여부와 Secrets의 `BPA_ADMIN_PASSWORD` 설정 여부 확인
