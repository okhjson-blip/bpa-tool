# BPA Tool 로컬 테스트 가이드

## 1. 실행

PowerShell에서 프로젝트 루트로 이동한 뒤 실행합니다.

```powershell
cd D:\hjson_tool\bpa-tool
npm.cmd run dev:local
```

환경 점검 후 UI와 API가 함께 실행됩니다.

- UI: http://localhost:3000
- API 상태: http://localhost:5000/api/health
- 종료: `Ctrl+C`

## 2. 첫 화면

1. 회사명을 입력하고 등록합니다.
2. Gemini, OpenAI, Claude 중 하나를 선택합니다.
3. 선택 공급자의 API Key를 입력하고 `AI API 등록`을 누릅니다.
4. 실제 모델 코드가 포함된 연결 성공 메시지를 확인합니다.
5. API Key는 Supabase나 브라우저 저장소에 저장되지 않으므로 새로고침 후 다시 등록해야 합니다.

## 3. 분석 흐름

1. 새 프로젝트를 만듭니다. 프로젝트에는 첫 화면에서 선택한 AI 엔진이 저장됩니다.
2. L4 과제를 등록합니다.
3. 인터뷰 답변을 입력하고 AI Draft를 생성합니다.
4. L4~L6 Draft가 표시되고 저장 완료 메시지에 실제 모델 코드가 나오는지 확인합니다.
5. 프로세스 수행시간·대기시간 등을 수정한 후 플로우차트를 동기화합니다.
6. BDW 진단을 실행하고 플로우차트 노드 태그와 진단표가 함께 바뀌는지 확인합니다.
7. AI FIT 분석을 실행해 점수·분류·추천 기술과 To-Be 데이터가 생성되는지 확인합니다.

프로젝트에 저장된 엔진과 현재 세션에서 등록한 엔진이 다르면 분석은 차단됩니다. 첫 화면에서 프로젝트와 동일한 엔진의 Key를 다시 등록하세요.

## 4. 결과 리포트

1. 결과 리포트로 이동합니다.
2. 프로젝트명과 과제명이 맞는지 확인합니다.
3. 연간 수행 횟수를 변경해 연간 절감시간과 FTE가 다시 계산되는지 확인합니다.
4. PDF에 포함할 섹션을 선택하고 `선택 섹션 PDF 출력`을 누릅니다.
5. 열린 인쇄 창에서 대상을 `PDF로 저장`으로 선택합니다.
6. `고정 항목 CSV 출력`을 누르고 다음 네 열만 포함되는지 확인합니다.
   - 프로젝트명
   - 과제명
   - AS-IS 프로세스
   - To-Be 프로세스

## 5. 문제 확인

- 환경 오류: `npm.cmd run doctor`
- Supabase 오류: `http://localhost:5000/api/health` 응답 확인
- 화면 오류: 브라우저 개발자 도구의 Console과 Network 확인
- 포트 충돌: 3000번 또는 5000번을 사용 중인 프로그램을 종료한 후 다시 실행
- AI 오류: 프로젝트 엔진과 첫 화면에서 선택한 엔진이 같은지, 해당 모델 사용 권한이 있는지 확인
