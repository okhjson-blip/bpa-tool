# BPA Tool UX 시나리오 설계

## 목적과 사용자

- 목적: 업무 프로세스를 분석하고 AI 자동화 및 재설계 후보를 도출
- 사용자: 시스템 관리자, 협력사 컨설턴트와 현업 담당자
- 환경: 데스크톱 브라우저, 14인치 전체 화면 최적화
- 인증: 협력사 Supabase 익명 세션 자동 복원, 관리자 비밀번호 로그인

## 전체 흐름

```text
접속 첫 화면
  → 협력사: 협력사 선택 + 이름·이메일 입력 + 시작하기(기존 사용자 접속 / 미등록 사용자 등록 안내)
  → 미등록 사용자: 안내 확인 + 등록 버튼 클릭 + 데이터베이스 등록 후 접속
  → 관리자: 관리자 비밀번호 로그인
  → Supabase 세션 복원 및 멤버십 확인
프로젝트 목록(로그인 협력사 데이터만)
  → 프로젝트 생성 또는 선택
과제 목록
  → L4 모듈 과제 등록 또는 선택
Step 1 과제 등록
  → Step 2 인터뷰 & Draft
  → Step 3 프로세스 수정
  → Step 4 BDW 진단
  → Step 5 AI FIT 분석
  → Step 6 결과 리포트
```

## 접속 첫 화면과 가입

- 관리자가 등록한 활성 협력사 목록에서 소속 협력사를 선택합니다.
- 이름과 이메일을 입력하고 `시작하기`를 클릭하며, 이메일 형식이 올바르지 않으면 등록하거나 접속하지 않습니다.
- 이메일 승인 없이 등록 버튼을 누르면 즉시 익명 인증 세션, 프로필, 협력사 멤버십을 생성합니다.
- 재접속 또는 새로고침 시 Supabase 세션을 자동 복원하고 `company_memberships`를 조회합니다.
- `시작하기`는 협력사·이메일의 기존 등록 여부만 확인합니다. 기존 사용자는 현재 Supabase 세션에 멤버십을 연결해 접속하고, 미등록 사용자는 등록 안내와 `등록` 버튼을 표시합니다. 사용자가 `등록`을 클릭한 경우에만 프로필과 멤버십을 생성한 뒤 프로젝트 목록으로 이동합니다.
- 활성 멤버십이 없거나 협력사 컨설팅이 완료 처리된 경우 프로젝트 화면에 진입할 수 없습니다.
- 외부 AI API 등록은 로그인 화면과 분리합니다. 로그인 후 상단 `AI API 관리`에서 협력사별로 등록하며 연결 확인 성공 후 암호화 저장합니다.
- 저장된 Key 원문은 다시 표시하지 않고 마스킹된 끝 4자리와 확인 모델만 표시합니다.
- 여러 엔진을 등록한 경우 새 프로젝트의 기본 엔진을 협력사 설정에서 지정합니다.

## 관리자 모드

- Supabase Edge Function Secret에 등록된 관리자 비밀번호가 일치할 때만 관리자 모드에 진입합니다. 비밀번호는 Vercel 또는 브라우저에 저장하지 않습니다.
- 관리자 모드는 `협력사/과제 관리`와 `협력사/사용자 관리` 두 페이지로 구분합니다.
- 사용자 관리에서는 협력사, 사용자 이름, 이메일 주소, 최근 접속 시간을 조회하고 사용자를 선등록·수정·삭제합니다. 선등록 사용자는 같은 협력사와 이메일로 처음 접속하면 현재 Supabase 익명 세션에 자동 연결되며 자유 가입도 계속 허용합니다.
- 협력사명과 컨설팅 차수(연도·상/하반기)를 등록하면 협력사 등록 화면의 선택 목록에 차수와 함께 나타납니다.
- 전체 협력사의 프로젝트·과제를 협력사별로 펼쳐 조회합니다.
- 프로젝트·과제는 조회 전용이며 관리자 화면에서 수정·삭제 기능을 제공하지 않습니다.
- 협력사·프로젝트·과제 통계는 각각 전체와 활성 건수를 구분해 표시합니다.
- 협력사를 완료 처리하면 소속 프로젝트와 과제도 함께 완료 처리되고 해당 협력사 사용자의 업무 데이터 접근이 즉시 차단됩니다. 다시 활성화하면 프로젝트와 과제는 완료 처리 직전의 진행·완료 상태로 복원됩니다.
- 협력사 모드 결과 페이지에서 출력 형식 선택 위에 PDF 프리뷰를 표시합니다. 사용자가 프리뷰를 확인하고 `결과 리포트 저장`을 누른 경우에만 해당 과제의 최신 PDF 리포트를 DB에 저장합니다. 관리자 과제 목록은 미저장 과제의 `상세 조회`를 비활성화하고 저장된 과제만 활성화합니다. 관리자는 `목록 새로고침` 후 활성화된 버튼을 눌러 저장본을 팝업으로 조회하며 리포트를 다시 계산하거나 변경하지 않습니다.
- `CSV 파일 다운로드`를 누르면 컨설팅 연도·반기 선택 팝업을 표시하고, 선택한 차수의 협력사명·프로젝트명·과제명을 과제별 한 행으로 출력합니다.

## 프로젝트 목록

- 로그인 사용자의 활성 멤버십에 연결된 `company_id` 프로젝트만 표시
- L1 구분·조직 기능, L2 대분류·업무 도메인, L3 중분류 프로젝트명, 목적, 달력 기간, 참여자, 과제 진행 현황, 최근 수정일 표시
- 각 카드의 `정보 수정`에서 기존 프로젝트 기본 정보와 참여자를 변경
- 각 카드의 `삭제`는 `삭제하시겠습니까?` 확인창에서 `네`를 선택한 경우에만 프로젝트와 소속 과제·분석·리포트를 삭제하며 `취소` 시 아무 작업도 하지 않음
- `+ 새 프로젝트`에서 회사명, L1~L3, 시작·종료일, 목적 입력 및 STATIK L1~L6 정의 팝업 확인
- 프로젝트 참여자 역할: 프로젝트 리더, 프로젝트 담당자, 컨설턴트. 세 역할이 최소 1명씩 없으면 보완사항 팝업 표시

## STATIK L1~L6 명칭과 정의

- `L1 구분 · 조직 기능`: 기업 밸류체인 영역이며 이 일이 속한 큰 조직 기능 단위입니다. 예: 연구개발, 제조, 영업마케팅, 경영지원
- `L2 대분류 · 업무 도메인`: 조직 기능별 핵심 업무이며 L1 내에서 구분되는 업무 도메인입니다. 예: S/W개발, 공정관리, 마케팅기획, 인사
- `L3 중분류 · 핵심 기능`: 업무 도메인을 구성하는 핵심 기능이자 목적별 세부 기능이며 분석 단위가 되는 핵심 기능 영역입니다. 예: 프런트엔드 개발, 품질검사, 콘텐츠 제작, 채용
- `L4 모듈`: 독립 업무가 연결되어 완성되는 업무 모듈이며 실제로 관리·인식되는 관련 업무의 묶음입니다. 담당자가 "이 업무를 담당한다"고 말하는 과제 등록 단위입니다. 예: UI 구현, SMT 공정, SNS 채널 관리
- `L5 단위 · 독립 업무`: "이 일을 했다"고 말할 수 있는 독립적 결과물이며 완료 여부를 명확히 확인할 수 있는 단위입니다. 예: 게시물 기획, 게시물 제작, 게시물 승인, 게시물 발행
- `L6 Act · 최소 행위`: 더 이상 쪼갤 수 없는 사람의 실제 행동 단위이며 수행 시간(분)을 반드시 입력하는 BDW 진단 기준 단위입니다. 예: 트렌드 키워드를 검색한다, 초안을 작성한다, 게시물을 업로드한다

## 과제 목록과 Step 1

- 과제는 L4 모듈로 프로젝트에 등록
- L1 구분·조직 기능, L2 대분류·업무 도메인, L3 중분류·핵심 기능은 선택한 프로젝트 정보에서 읽기 전용으로 연동
- L4 업무 모듈, 과제명, 달력 시작·종료일, 목표 및 과제 참여자 입력
- 과제 시작·종료일은 상위 프로젝트 시작·종료일 범위 안에서만 선택·등록 가능하며 범위를 벗어나면 안내 후 등록 차단
- 과제 참여자 역할: 과제 리더, 과제 담당자. 두 역할이 최소 1명씩 없으면 보완사항 팝업 표시
- 등록 과제를 다시 열고 이전 단계로 이동하면 과제 기본정보를 Supabase에서 자동 로딩하며 수정 후 정식 저장 가능
- 결과 리포트가 저장된 과제는 사용자·관리자 목록 모두 `완료` 상태로 표시

## Step 2 인터뷰 & Draft

기본 질문은 업무 순서와 시간, 반복·적체, 승인·대기, 불필요 작업, 고객 불편을 다룹니다. 답변을 텍스트로 입력하면 AI Draft가 등록 과제의 L1~L4와 STATIK 정의에 따라 L4 모듈·L5 단위·L6 Act 구조를 생성합니다. L6 Act는 `트렌드 키워드를 검색한다`처럼 목적어와 단일 동사로 구성합니다.

등록 과제의 인터뷰 단계로 다시 이동하면 과제에 연결된 최신 구조화 답변과 저장된 AI Draft를 Supabase에서 자동 로딩하고, 임시 저장 답변이 있으면 이를 우선 표시합니다.

AI Draft, BDW 진단, AI FIT 분석은 프로젝트에 등록된 엔진과 협력사에 암호화 저장된 같은 엔진의 API Key를 사용합니다. Key가 없거나 복호화할 수 없으면 `AI API 관리`에서 등록 또는 교체하도록 안내합니다.

Draft 컬럼:

- 레벨
- 단위 업무명
- 작업방식
- 사용도구
- 수행시간(분)
- 승인대기(h)

작업방식과 사용도구는 L6 Act에만 적용하며 L4 모듈·L5 단위에는 표시하거나 저장하지 않습니다. L6 Act의 수행·대기·승인대기 합계는 상위 L5에, L5 합계는 상위 L4에 자동 반영합니다.

## Step 3 프로세스 수정

- 좌측 60% 편집 테이블, 우측 40% 플로우차트
- 행 추가, 이동, 삭제, 레벨 변경
- 테이블과 플로우차트의 동일 데이터 사용
- 프로세스 플로우의 L6 Act 노드만 단위 업무명 아래에 작업방식·도구를 표시하고, L4·L5 노드는 시간 합계만 표시
- 모든 변경 프로세스를 단일 API 요청으로 저장하여 다중 인증 요청으로 인한 일시적 실패 방지
- `저장` 버튼으로 프로세스 수정 임시 저장 상태 표시

## Step 4 BDW 진단

- Bottleneck: 처리량을 제한하는 적체 구간
- Delay: 대기·승인·전달 시간 손실
- Waste: 고객 가치에 기여하지 않는 활동
- 정상: 비효율 태그 없음

상단에 태그별 개수와 비효율 지수를 표시하고 테이블에서 태그를 수정합니다. L6 Act 목록은 프로세스 편집·플로우차트와 같은 저장 순서로 표시합니다.

## Step 5 AI FIT 분석

- AI 적용 가능성 1~5
- 비효율성 1~5
- A: 즉시 적용
- B: 수동 개선 우선
- C: 장기 검토
- D: 현상 유지
- 난이도: 추천 AI 기술을 비개발자가 구현하는 관점에서 상·중·하

인라인 분석, 2×2 매트릭스, As-Is/To-Be 비교 탭을 제공합니다. 비교 화면만 좌우 1:1 비율을 사용합니다.

인라인 분석에서는 프로세스 순서의 L6 Act 행마다 수락 체크박스를 제공합니다. `AI 제안 선택 수락 후 To-Be 반영`은 체크한 행만, `AI 제안 전체 수락 후 To-Be 반영`은 모든 행을 적용합니다. 적용 결과는 As-Is와 To-Be의 좌우 플로우차트로 표시하고 양쪽 노드에 작업방식 배지와 도구·수행시간 보조 문장을 제공합니다. To-Be에서 자동화되는 업무는 `AI 자동화` 또는 `시스템 자동화` 배지로 구분하고 미수락 수작업은 현행 유지로 표시합니다.

As-Is 작업방식 배지는 원본 프로세스의 수작업·시스템 값을 그대로 표시하며 To-Be의 AI 자동화 배지를 사용하지 않습니다.

## Step 6 결과 리포트

- STATIK 구조
- As-Is 프로세스
- BDW 결과
- AI FIT 및 To-Be
- FTE 성과 지표
- 과제 참여자(이름·직급·역할·이메일)
- 선택적 변경 이력

출력은 두 가지입니다. PDF는 별도 섹션 선택 없이 STATIK, 과제 참여자, L6 기준 AS-IS, BDW 요약·전체 목록, AI FIT·To-Be, AX 성과지표 전체를 프리뷰와 인쇄 화면에 동일하게 표시합니다. AS-IS·To-Be 표에는 작업방식과 도구 컬럼을 포함합니다. `결과 리포트 저장`은 현재 프리뷰의 서버 검증 데이터를 DB에 저장합니다. DB 이관용 CSV는 저장된 결과 리포트의 식별 메타데이터, 각 PDF 섹션 JSON, 원본 `report_data_json`을 과제당 한 행으로 출력하며 다른 DB에서 동일 리포트를 복원할 때 사용합니다. 보고서 제목 키인은 제공하지 않습니다. 수행 빈도는 일·주·월·년과 기간당 횟수를 입력하고, 연간 횟수로 환산한 AX 절감시간을 2,248시간으로 나누어 절감 FTE를 계산합니다. FTE 기반 버튼을 누르면 산식과 기준 설명 팝업을 표시합니다.

## 공통 UX

- 프로젝트 기본정보, 과제 기본정보, 인터뷰 답변, 프로세스 수정, 수행 빈도 패널에만 `저장` 버튼을 표시합니다. 클릭 시 Supabase에 임시 저장하고 재접속 시 로그인 사용자의 최신 저장본을 자동 로딩합니다. 로딩 후 계속 수정·재저장할 수 있으며 정식 등록 또는 분석 저장 완료 시 관련 임시 저장본을 정리합니다.
- 과제 목록에서 완료 과제의 `과제 상세`은 저장된 결과 리포트를 팝업으로 표시하며, 완료 과제 카드는 6단계 결과 리포트로 이동합니다. 진행 중 과제 카드는 마지막으로 저장한 `current_step`과 해당 단계의 Supabase 데이터를 복원합니다.
- 모든 과제 카드에 `과제 상세`과 동일한 크기의 `삭제` 버튼을 표시합니다. `삭제하시겠습니까?` 확인창에서 `네`를 선택한 경우에만 과제와 관련 분석·리포트 데이터를 Supabase에서 삭제하며 `취소`하면 유지합니다.
- 기본정보 필수 입력란이 비어 있으면 보완 항목을 안내하고 정식 등록·다음 단계 이동을 차단합니다.
- BDW 태그처럼 이미 즉시 DB 저장되는 항목은 중복 임시 저장하지 않고 저장 상태만 표시합니다. AI API Key는 기존 암호화 저장을 유지하며 임시 저장에서 제외합니다.
- 사용자 모드 상단에는 협력사명 옆에 현재 접속자 이름을 표시합니다.

- 목록 화면에서는 단계 바와 하단 단계 푸터를 숨김
- 단계 화면에서는 프로젝트와 과제 브레드크럼, 6단계 바, L1~L4 컨텍스트 칩 표시
- 프로젝트 생성·수정 및 Step 1~3에서 STATIK 계층 정의 팝업 제공
- Step 5에서 AI FIT 점수 척도 팝업 제공
- AI FIT 비효율성 척도는 1(효율적)을 초록색, 5(매우 비효율적)를 붉은색으로 표시
- Step 6에서 과제 목록 및 프로젝트 목록으로 이동 가능
